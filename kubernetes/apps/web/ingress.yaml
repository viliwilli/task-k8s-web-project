apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web
  namespace: web
  labels:
    app.kubernetes.io/name: web
spec:
  ingressClassName: traefik
  rules:
    - host: web.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 80
